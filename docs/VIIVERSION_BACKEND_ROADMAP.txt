VIIVERSION BACKEND — ROADMAP
Version: 1.2
Date: 2026-09-09
Branch: backend

ARCHITECTURAL DECISION

The first production version is NOT multi-tenant.
Each client receives a separate isolated backend deployment.

For every client:
- separate Cloudflare Worker;
- separate Cloudflare D1 database;
- separate R2 bucket when file storage is required;
- separate KV namespace when cache/config storage is required;
- separate Queues / Cron / Workflows when required;
- separate environment variables and secrets;
- separate Telegram bot token;
- separate payment provider credentials;
- separate domain/subdomain and deployment pipeline;
- separate migrations and production data.

Reusable code may be reused between client projects, but clients do not share runtime databases, secrets, queues or business data.
Multi-tenancy is explicitly postponed until several isolated production implementations prove which components are genuinely common.

TARGET STACK

- TypeScript
- Cloudflare Workers
- Hono
- Zod
- Cloudflare D1
- Drizzle ORM
- Cloudflare R2
- Cloudflare KV
- Cloudflare Queues / Cron Triggers / Workflows where required
- Telegram Bot API / Telegram Mini Apps initData validation
- REST JSON API
- Vitest
- Wrangler
- GitHub Actions CI

REPOSITORY STRATEGY

Current development branch: backend

The branch contains the reference backend architecture and first production-ready backend foundation.
Client-specific backends will be derived from this architecture and deployed separately.

STAGE 0 — ARCHITECTURE AND RULES
Status: COMPLETED — 2026-09-09

Completed:
- isolated-per-client architecture fixed;
- backend branch created;
- backend README created;
- roadmap created;
- secret/environment files excluded from git;
- no tenant_id / shared client database model introduced.

STAGE 1 — BACKEND FOUNDATION
Status: COMPLETED — 2026-09-09

Implemented:
- Cloudflare Worker TypeScript entrypoint;
- Hono routing;
- Zod runtime environment validation;
- request IDs;
- structured JSON request logging;
- CORS allowlist;
- centralized 404/error responses;
- REST API version prefix /api/v1;
- GET /health;
- GET /api/v1/status;
- Vitest foundation tests;
- strict TypeScript configuration;
- Wrangler configuration;
- GitHub Actions backend CI.

Files:
- package.json
- tsconfig.json
- wrangler.jsonc
- .gitignore
- src/index.ts
- src/app.ts
- src/config.ts
- src/types.ts
- vitest.config.ts
- test/app.test.ts
- .github/workflows/backend-ci.yml

Verification:
- PASS — npm dependency installation in GitHub Actions;
- PASS — TypeScript typecheck;
- PASS — Vitest tests;
- PASS — Wrangler deploy --dry-run Worker build;
- PASS — static TypeScript syntax check;
- PASS — no credentials committed to git.

STAGE 2 — DATABASE FOUNDATION (D1)
Status: NEXT

Goals:
1. Add Cloudflare D1 binding strategy for isolated client deployments.
2. Add Drizzle ORM and schema configuration.
3. Add versioned migrations.
4. Add base tables.
5. Add migration scripts and documentation.
6. Separate local / preview / production database configuration.

Base tables:
- users
- admins
- auth_sessions or auth metadata where required
- audit_log
- app_settings

Rules:
- one D1 database per client deployment;
- no tenant_id column required;
- migrations versioned in git;
- no production database identifiers/secrets hard-coded into reusable source;
- business tables added only for the selected client module.

Acceptance criteria:
- schema typechecks;
- migrations can create an empty database from zero;
- Worker can access D1 through typed binding;
- local database flow documented;
- per-client production binding procedure documented;
- CI remains green.

STAGE 3 — TELEGRAM AUTHENTICATION
Status: PENDING

Goals:
- POST /api/v1/auth/telegram;
- GET /api/v1/me;
- server-side Telegram initData signature validation;
- initData expiry validation;
- user create/update in D1;
- protected-route middleware;
- Telegram bot token stored only as Cloudflare secret.

STAGE 4 — ADMIN AUTHORIZATION / RBAC
Status: PENDING

Roles:
- owner
- admin
- manager

Requirements:
- server-side permission checks;
- audit trail for sensitive actions;
- frontend role checks never treated as security.

STAGE 5 — CLIENT BUSINESS MODULE
Status: PENDING

Implemented separately for each client.

Tour operator example (RIC / Eco Voyage / Max Tour):
- tours
- tour_dates
- availability
- prices
- bookings
- booking_items
- customers
- promocodes

Rental example (UNIQ):
- vehicles
- categories
- availability
- rental_prices
- rentals
- customers

Clinic example (PET NIKA / AVE):
- doctors
- services
- pets
- appointments
- customers

Rule:
Only tables and logic required by the specific client's business are deployed.

STAGE 6 — BOOKING / ORDER ENGINE
Status: PENDING

Goals:
- server-side price calculation;
- availability validation;
- atomic order/booking creation;
- explicit status state machine;
- idempotency protection;
- duplicate-request protection.

Typical statuses:
- draft
- pending_payment
- paid
- confirmed
- cancelled
- completed
- refunded

STAGE 7 — PAYMENTS
Status: PENDING

Goals:
- payment provider adapter;
- server-side payment creation;
- signed webhook verification;
- idempotent webhook processing;
- paid status only after verified provider event;
- refund flow when supported.

STAGE 8 — TELEGRAM BOT / NOTIFICATIONS
Status: PENDING

Goals:
- bot webhook;
- /start;
- Mini App launch flow;
- booking/order confirmation;
- manager notifications;
- scheduled reminders;
- deep links;
- safe retry behavior.

STAGE 9 — ADMIN / CRM API
Status: PENDING

Goals:
- customers;
- bookings/orders;
- catalog;
- prices;
- availability;
- managers;
- roles;
- settings;
- CRM history;
- audit log.

STAGE 10 — FILES / MEDIA
Status: PENDING

Goals:
- R2 integration;
- validated uploads;
- file type/size restrictions;
- metadata/URLs in D1;
- safe replacement/deletion.

STAGE 11 — BACKGROUND JOBS
Status: CLIENT-DEPENDENT

Possible technologies:
- Cloudflare Queues
- Cron Triggers
- Workflows

Use cases:
- reminders;
- unpaid booking expiration;
- post-purchase messaging;
- notification retries;
- integration retries;
- report generation.

STAGE 12 — ANALYTICS
Status: PENDING

Typical events:
- app_open
- product_view
- booking_started
- booking_created
- payment_started
- payment_success
- purchase

Rule:
Analytics is never the source of truth for orders/payments.

STAGE 13 — EXTERNAL INTEGRATIONS
Status: CLIENT-DEPENDENT

Possible integrations:
- website / Tilda;
- CRM;
- payment systems;
- Google services;
- messaging providers;
- partner APIs;
- accounting/export systems.

All credentials are isolated per client.

STAGE 14 — SECURITY HARDENING
Status: PENDING

Checklist:
- Telegram initData signature validation;
- Zod validation;
- RBAC;
- secrets outside git;
- webhook verification;
- rate limiting;
- CORS allowlist;
- idempotency;
- typed DB access;
- audit log;
- sanitized error responses;
- dependency audit;
- backup/export procedure;
- logs without secret/PII leakage.

STAGE 15 — AUTOMATED TESTING
Status: STARTED

Already implemented:
- foundation API tests.

Required later:
- Telegram auth tests;
- request validation tests;
- booking price tests;
- availability/concurrency tests;
- payment webhook/idempotency tests;
- RBAC tests;
- migration tests.

STAGE 16 — CI/CD / CLOUDFLARE DEPLOYMENT
Status: CI FOUNDATION STARTED

Already implemented:
- GitHub Actions on backend push/PR;
- dependency installation;
- typecheck;
- tests;
- Wrangler dry-run build.

Later:
- preview deployment;
- production deployment;
- D1 migration procedure;
- deployment checklist;
- per-client secrets/bindings.

Client isolation example:

RIC:
- Worker: ric-backend
- D1: ric-production
- R2: ric-media

UNIQ:
- Worker: uniq-backend
- D1: uniq-production
- R2: uniq-media

No runtime dependency between deployments.

STAGE 17 — FIRST REAL CLIENT IMPLEMENTATION
Status: PENDING

Recommended first implementation:
Tour/travel backend because it exercises catalog + schedule + availability + booking + payments + CRM + Telegram notifications.

Only after several separate production backends prove common components should reusable packages be extracted.

DEVELOPMENT PRINCIPLES

1. Build production behavior, not demo-only backend behavior.
2. Never trust prices, roles, payment state or Telegram identity from the frontend.
3. Keep every client's production data and credentials isolated.
4. Prefer Cloudflare-native services before introducing external infrastructure.
5. Multi-tenancy is postponed.
6. Every stage must pass acceptance criteria before it is marked complete.
7. No secrets, bot tokens, API keys, payment credentials or production personal data in git.
8. Schema migrations are versioned.
9. API is versioned under /api/v1.
10. Auth/payment/security changes require tests.
11. CI must remain green before proceeding to production deployment.

CURRENT EXECUTION ORDER

[DONE] Stage 0 — architecture and rules
[DONE] Stage 1 — Worker / TypeScript backend foundation
[NEXT] Stage 2 — D1 / Drizzle / migrations
[PENDING] Stage 3 — Telegram authentication
[PENDING] Stage 4 — admin RBAC
[PENDING] Stage 5 — first client business module
[PENDING] Stage 6 — booking/order engine
[PENDING] Stage 7 — payments
[PENDING] Stage 8 — Telegram notifications
[PENDING] Stage 9 — CRM/admin API
[PENDING] Stages 10–16 as required
[PENDING] Stage 17 — first production client implementation
