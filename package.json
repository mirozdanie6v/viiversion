{
  "name": "viiversion-landing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "bash scripts/restore-site.sh && python3 scripts/remove-projects.py && python3 scripts/add-prototype-links.py",
    "deploy": "wrangler deploy",
    "dev": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
