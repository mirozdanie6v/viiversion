{
  "name": "viiversion-landing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "bash scripts/restore-site.sh && python3 scripts/update-home-hero.py && python3 scripts/update-about-section.py && python3 scripts/update-home-visual.py && python3 scripts/remove-projects.py && python3 scripts/add-prototype-links.py && python3 scripts/add-pet-nika-case.py && python3 scripts/add-uniq-case.py && python3 scripts/responsive-site.py && python3 scripts/verify-prototype-links.py",
    "deploy": "wrangler deploy && bash scripts/smoke-public.sh",
    "dev": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
