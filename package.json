{
  "name": "viiversion-landing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "bash scripts/restore-site.sh",
    "deploy": "wrangler deploy",
    "dev": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
